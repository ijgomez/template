import { ShowMenuItemPipe } from './show-menu-item.pipe';

describe('ShowMenuItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMenuItemPipe();
    expect(pipe).toBeTruthy();
  });
});
