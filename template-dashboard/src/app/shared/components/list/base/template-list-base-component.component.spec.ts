import { TemplateListBaseComponent } from './template-list-base-component.component';

describe('TemplateListBaseComponent', () => {
  it('should create an instance', () => {
    expect(new TemplateListBaseComponent()).toBeTruthy();
  });
});
