import { TemplateFormBaseComponent } from './template-form-base-component.component';

describe('TemplateFormBaseComponent', () => {
  it('should create an instance', () => {
    expect(new TemplateFormBaseComponent()).toBeTruthy();
  });
});
