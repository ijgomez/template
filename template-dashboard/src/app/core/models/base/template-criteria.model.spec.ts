import { TemplateCriteria } from './template-criteria.model';

describe('TemplateCriteria', () => {
  it('should create an instance', () => {
    expect(new TemplateCriteria()).toBeTruthy();
  });
});
