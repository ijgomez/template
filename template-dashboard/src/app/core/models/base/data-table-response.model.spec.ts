import { DataTableResponse } from './data-table-response.model';

describe('DataTableResponse', () => {
  it('should create an instance', () => {
    expect(new DataTableResponse()).toBeTruthy();
  });
});
