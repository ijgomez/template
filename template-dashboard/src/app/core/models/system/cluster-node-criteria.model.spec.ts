import { ClusterNodeCriteria } from './cluster-node-criteria.model';

describe('ClusterNodeCriteria', () => {
  it('should create an instance', () => {
    expect(new ClusterNodeCriteria()).toBeTruthy();
  });
});
