import { PropertyCriteria } from './property-criteria.model';

describe('PropertyCriteria', () => {
  it('should create an instance', () => {
    expect(new PropertyCriteria()).toBeTruthy();
  });
});
