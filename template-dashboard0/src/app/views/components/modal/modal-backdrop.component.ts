import {Component} from '@angular/core';

@Component({
    selector: 'app-modal-backdrop',
    template: '',
    // tslint:disable-next-line:use-host-property-decorator
    host: {'class': 'modal-backdrop fade in'}
})
export class ModalBackdropComponent {
}
