# Estructura del Proyecto

> **Nota:** El proyecto es actualmente una plantilla vacía. Actualiza este archivo conforme se vayan añadiendo directorios y módulos.

## Estructura Actual

```
template/
├── .git/               # Repositorio Git (incluye LFS)
├── .kiro/
│   └── steering/       # Ficheros de guía para el asistente IA
└── README.md           # Readme del proyecto (aún sin rellenar)
```

## Convenciones

- Mantener los documentos de steering en `.kiro/steering/` y actualizarlos conforme evolucione el proyecto.
- El `.gitignore` sugiere que se añadirá un subproyecto llamado `template-dashboard`, probablemente un módulo frontend.
- Usar kebab-case para los nombres de directorios y seguir el modelo de ramas: `master` para código estable, `release/*` para candidatos a release.
