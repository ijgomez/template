/**
 * Data Transfer Objects (DTOs) for the domain model.
 * <p>
 * All DTOs are implemented as Java records (immutable value objects).
 * A single DTO per entity covers both create ({@code id = null}) and update ({@code id} populated) use cases.
 * </p>
 */
package org.myorganization.template.domain.dto;
