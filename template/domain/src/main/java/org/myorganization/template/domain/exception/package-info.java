/**
 * Domain exception classes for the template application.
 * <p>
 * These are pure Java exceptions (no Spring dependencies) that represent
 * business-level error conditions. The webapp module's GlobalExceptionHandler
 * maps them to appropriate HTTP status codes.
 */
package org.myorganization.template.domain.exception;
